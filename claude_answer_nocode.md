he user wants a complete demo that:

Reads/writes macOS utun interface
Passes packets to/from WireGuard processing (using the libwg.a library shown)
WireGuard processes packets and reads/writes UDP socket
For testing with a Linux VM

Let me write a complete demo in C that:

Creates/opens a utun interface on macOS
Reads plaintext IP packets from utun
Encrypts them via WireGuard (using the noise/crypto functions from libwg.a)
Sends encrypted packets over UDP
Receives encrypted UDP packets
Decrypts them back through WireGuard
Writes plaintext packets back to utun

Looking at the libwg.a library, I can see this is a static WireGuard implementation with noise protocol functions like the kernel-level state machine. Integrating directly with these internal functions gets complicated, so I'll take a simpler approach...
Writing the C demo... Writing the C demo... Writing the complete demo... I'm realizing the exact struct layouts from libwg.a aren't available to me, so I'll take a practical approach instead—using opaque pointers for the WireGuard state since that's how the actual API works anyway. My plan is to structure this as a complete, compilable demo with separate modules for the main event loop, the utun interface, and WireGuard session management.
Since the user wants everything in one or two files, I'll consolidate it and declare the opaque types and extern functions without needing the full struct definitions. I'm also calculating the WireGuard message buffer sizes: initiation messages are 148 bytes, response messages are 92 bytes, and data messages have a 4+4+8 byte header plus payload and authentication tag.
Writing the complete demo... Writing the WireGuard interface code... Looking at more function signatures from the noise.h header to understand the full API surface for managing local and remote noise protocol instances. Defining the handshake message creation and consumption functions, along with keypair management operations for tracking and referencing active encryption keys.
Now I'm noticing this uses mbuf structures for encryption and decryption, which is a kernel-level abstraction typically found in BSD systems. That makes it tricky to use in userspace applications since mbuf is really a kernel concept, not something you'd normally encounter in regular user-facing libraries.
But wait—since this is a static library meant for linking, there's probably a userspace implementation of mbuf, or maybe the encrypt/decrypt functions have alternative interfaces that work with raw buffers. Looking at the crypto functions more carefully, I see there are two sets: the raw buffer versions like _chacha20_poly1305_encrypt and the mbuf versions like _chacha20poly1305_encrypt_mbuf. The fact that noise_keypair_encrypt/decrypt specifically take struct mbuf ** suggests this might be part of a Network Extension framework for macOS where mbufs are actually available.
Given how complex this is, I should write a practical example that uses the low-level crypto functions directly rather than trying to work with the mbuf-based interface.
Writing a complete demo... I'm realizing the keypair object doesn't expose the raw session keys directly, so I need to either wrap the library's mbuf-based encryption functions or implement the noise protocol manually using just the underlying crypto primitives like blake2s, chacha20, and curve25519. The manual approach seems cleaner for a demo since it gives me full control over the handshake and data path. Writing WireGuard implementation... Defining the data packet structure and now moving into the noise protocol implementation with the key handshake functions that handle initiation, response creation, and session key derivation. Implementing the Noise_IKpsk2 protocol handshake steps for the initiator, where I'm deriving keys, encrypting the static public key, and building the initial message with the ephemeral key and encrypted payload.
Continuing the responder's side...
Now I'm working through the key derivation functions that WireGuard defines—KDF1, KDF2, and KDF3 all built on BLAKE2s—to derive the final session keys for sending and receiving. I'm realizing the standard HMAC construction applies to any hash function, so I can use it with BLAKE2s. Now I'm going to write out the complete implementation as a single C file with a Makefile, building a full WireGuard demo for macOS that connects the utun interface with the Noise protocol and UDP socket.
Writing the implementation... Writing the implementation code... Defining the blake2s hash structure and declaring the external crypto functions...
Still declaring crypto functions... Defining the WireGuard message structures... Defining the cryptographic helper functions... Still writing HMAC implementation... Continuing with the KDF implementation... Finishing the HKDF expansion and moving into the WireGuard handshake implementation—I'm setting up helper functions for hashing and timestamp generation using BLAKE2s and TAI64N format. Still writing handshake initialization... Still writing handshake logic... Still writing cryptographic operations... Setting up the hash state and computing the initial hash values for the handshake, then deriving the MAC1 key from our static public key to verify the initiator's message authentication code.
Computing the MAC1 verification...
Continuing with the Diffie-Hellman computation and key derivation, then decrypting the sender's static public key using ChaCha20-Poly1305 with the derived key, and finally verifying it matches the expected peer key before hashing the decrypted static data. Finishing the timestamp hash update, then I'm saving the handshake state to the peer structure—storing the chaining key, hash, and the initiator's ephemeral public key—along with extracting the remote session index and clearing the temporary DH and key material before returning.
Now I'm implementing the response message creation function, which sets up the local session index and message headers with the appropriate indices, then generates a fresh ephemeral keypair for the responder.
I'm realizing there's a structural problem here—when processing the initiator's message, I saved their ephemeral key in a field that I'm now overwriting with our own ephemeral key for the response. I need to separate these two keys by adding distinct fields to the peer structure so I can keep track of both the initiator's ephemeral public key and our own ephemeral key independently.
Restructuring the peer struct...
Now I'm working through the WireGuard handshake protocol carefully, referencing the whitepaper to get the message format and cryptographic operations exactly right for both initiator and responder sides. I need to check the WireGuard whitepaper more carefully for the exact KDF definitions—the notation I'm using doesn't match what's actually specified, and I should verify the implementation details before proceeding with the handshake steps. Continuing with the output expansion logic...
Now I'm looking at the KDF structure—it extracts a secret using HMAC-BLAKE2s with the key and data, then expands it into multiple outputs by iteratively hashing with counter values. The mix_hash function takes an existing hash and combines it with new source data through BLAKE2s hashing.
Implementing HMAC-BLAKE2s...
Wait, I need to double-check the function signature for blake2s in the library—it takes parameters in the order (out, in, key, outlen, inlen, keylen), but I'm calling it differently here. Let me verify the correct parameter order.
Actually, looking at the available functions, the library provides a streaming API with init, update, and final operations. I should rewrite the HMAC implementation using that approach instead.
Writing HMAC implementation... Finishing up the HMAC implementation and now moving into the KDF function that takes the HMAC output and derives multiple keys from it using the Noise protocol pattern. Writing the Noise handshake protocol... I'm working through the responder's side of the protocol now, verifying the MAC1 and then decrypting the static key using the ephemeral DH shared secret. But I'm catching an issue—the third DH computation with the responder's static key against the initiator's static key needs to happen after I've decrypted and verified the initiator's public key, not before. Building the responder's response message...
Now I'm ready to write out the complete C implementation cleanly, making sure to handle the cryptographic operations correctly. I need to check the exact function signatures for the AEAD cipher from the library I'm using.
Writing the complete implementation... Writing the WireGuard demo code... Defining the BLAKE2s hash state structure and declaring the external cryptographic functions from the WireGuard library... Defining the encryption and decryption functions for ChaCha20-Poly1305, along with the key constants used throughout the cryptographic operations.
Now I'm setting up the Noise protocol configuration strings and WireGuard message type identifiers, then laying out the structure for the initial handshake message.
Defining the response and data packet structures...
Now I'm laying out the session state that tracks encryption keys, nonces, and indices for each peer connection, along with the peer's static key material and configuration flags.
Defining the handshake working state... Writing HMAC implementation... Continuing with the KDF implementation...
I'm finishing up the key derivation function by copying the output values and clearing sensitive data, then moving into a timestamp function that reads the current time using the system clock.
Now I'm implementing the MAC1 key generation by hashing a label with the public key, and then setting up the function to compute the MAC1 value over a message. Writing cryptographic initialization code... Continuing with the Noise protocol handshake, I'm performing the Diffie-Hellman exchange between the ephemeral and peer's static key, deriving the encryption key through KDF, then encrypting the initiator's static public key with authenticated encryption and mixing it into the hash state. Finishing up the initialization message by computing the MAC1 value and clearing sensitive data, then moving into the responder's side of the handshake where it needs to validate and decrypt the incoming initiation message. Continuing with the Noise protocol handshake, I'm hashing the ephemeral key, then performing a Diffie-Hellman operation between the responder's static key and the initiator's ephemeral key to derive the next cipher state. After decrypting the static key from the message, I need to verify it matches the expected peer public key to authenticate the initiator.
Now I'm mixing the encrypted static key into the hash, performing another DH operation between static keys to update the cipher state again, and finally decrypting the timestamp payload to complete the message processing.
After verifying the timestamp decryption succeeds, I'm mixing the encrypted timestamp into the hash and saving the handshake state—storing the chain key, hash, peer's ephemeral public key, and their message index for the next phase. Then I'm clearing the temporary DH and key buffers before returning success.
Now I'm implementing the responder side of the handshake, where I need to set up the local session index and message fields, generate my own ephemeral key pair, and update the chain key based on my ephemeral public key.
Continuing with the Noise protocol operations... Finishing the key derivation and session setup, then computing the MAC1 value and clearing sensitive data from memory.
Now I'm moving into the responder's side of the handshake, where it processes the initiator's response message by deriving shared secrets through a series of Noise protocol KDF operations and hash updates. Continuing with the cryptographic operations, I'm completing the pre-shared key derivation and then verifying the empty payload field using authenticated encryption. Once that verification passes, I derive the session keys for both sending and receiving directions from the handshake cipher state.
Writing encryption logic... Writing decryption logic... Setting up the utun socket control structure...
Now I'm connecting to the utun interface and retrieving its name through socket options, then returning the file descriptor for the tunnel device.
I'm implementing the read function to strip the 4-byte address family header that utun prepends to packets, then copy the actual IP data into the caller's buffer. For the write function, I'm detecting whether the packet is IPv4 or IPv6 by examining the version field...
Setting up the address family header and using writev to send both the AF header and the packet data in one syscall. Now I'm moving on to implement UDP socket binding...
Creating a UDP socket, setting SO_REUSEADDR to allow quick rebinding, then binding to the specified port on all interfaces. After that I'm setting up hex encoding utilities...
Writing hex encoding logic... Writing the usage function... Parsing command-line arguments to handle key generation, initiator mode, local port configuration, peer address, and private key decoding with error handling. Setting the initiator flag and displaying the derived public key in hex format, then parsing the peer address to extract the IP and port for the initiator mode.
Now I'm validating the peer IP address, opening a TUN interface, and preparing to configure it with the appropriate IP settings. Setting up the handshake initiation for the WG protocol—if this is the initiator side, I'm creating and sending the initial message to the peer, then setting up an event loop to handle incoming packets.
Now I'm configuring a poll mechanism to monitor both the UDP socket and TUN device, with the TUN only being read when the session is actually ready, and handling timeouts to potentially resend the handshake if needed.
Handling incoming UDP packets...
Now I'm checking the message type and handling the initiation handshake—if I'm the responder and receive an initiation, I'll validate it and create a response message to send back.
Continuing with the response handling...
Now I'm handling the data message case where I decrypt the incoming packet and write the plaintext to the TUN interface, then checking for activity on the TUN device itself.
When the TUN interface has data ready, I read the packet, encrypt it using the peer's key, and send the encrypted payload back out over UDP to the peer's address.
Now I'm setting up the build configuration with a Makefile that compiles the demo against the WireGuard library, but I'm realizing I need to verify a few compatibility details—specifically the return types for the crypto functions and whether the blake2s state struct definition matches what the library expects, since a mismatch there could cause crashes. So the struct size varies depending on whether buflen is a size_t (8 bytes on 64-bit systems) or a uint32_t (4 bytes), which affects the total padding—either around 128 bytes or 120 bytes. Since this is an Apple library, I need to figure out which version they're using.
I'll go with uint32_t buflen to match the wireguard kernel implementation more closely. Now looking at the noise_kdf function when called with NULL data for empty key derivation—that should work fine with HMAC since the data is just empty. The mix_hash function also handles the zeroed PSK case correctly per the WireGuard spec, even though the KDF3 still runs. I need to verify that wg_consume_response properly saves and uses the initiator's ephemeral key.
Actually, I'm checking the DH computation in wg_consume_response—it's computing DH(Spriv_i, Epub_r), which matches what the responder computed, so the shared secret is correct. For the MAC computation, I'm using blake2s_init_key for BLAKE2s-MAC.
Looking at WireGuard's implementation, the MAC is just keyed BLAKE2s rather than HMAC-BLAKE2s. My compute_mac1 function uses blake2s_init_key which is the right approach for keyed BLAKE2s, and since MAC1 is 16 bytes, I'm initializing it with output length 16 and a 32-byte key, which should be correct. Now I need to verify the key size constraints for blake2s_init_key. Still writing the initialization code... Defining inline conversion functions for little-endian operations that are no-ops on x86 and ARM architectures. Now I'm putting together the final production version and need to verify the session key derivation logic against the WireGuard specification. The initiator's KDF call correctly produces send=T1 and recv=T2, matching the spec. Now I need to verify that both initiator and responder have the same chaining key value when they derive the session keys—let me trace through the responder's state after consuming the initiation message to confirm the chaining key matches what the initiator computed.
Verifying the KDF sequence... Now I'm verifying the DH computations match on both sides, which confirms the chaining keys will be identical and the session keys will derive correctly. I need to write out the complete implementation, making sure the Makefile properly links against libwg.a and handling the macOS-specific headers for the utun interface—particularly sys/kern_control.h for the control socket definitions and sys/sys_domain.h for the address family constants.
I'll define the utun control name and option constants directly since the public headers might not expose them, and I need to ensure the blake2s_state struct I'm using matches the binary layout from the WireGuard library implementation.
Writing the complete implementation... Writing the implementation...
Now I'm sketching out the Linux VM testing setup with WireGuard configuration on one end and the macOS demo running on the other to verify the implementation works across systems.
Actually, I need to make sure the demo implementation is fully compatible with the real WireGuard protocol—the handshake messages, session key derivation, and packet structure all need to match the spec exactly. Let me verify the data packet format is correct with the message type, receiver index, counter, and encrypted payload in the right order.
Looking at how the Linux implementation handles padding, I see it pads packets to 16-byte boundaries...
Verifying packet structure implementation... Looking at the actual WireGuard kernel implementation, I can see that encrypt_packet pads the plaintext to a 16-byte boundary before encryption, calculating the padding length as the difference between the aligned size and current packet length. I need to incorporate this padding logic into my wg_encrypt function to match the real behavior.
For decryption, the receiver gets the padded plaintext after decryption but relies on the IP header's length field to know the actual packet size, so when writing to utun I should use that length rather than the padded length. This means my encryption needs to pad to 16 bytes, then encrypt, and decryption needs to decrypt and extract the true packet length from the IP header.
Updating the wg_encrypt function... I need to verify the exact format for the AF header when reading from and writing to utun on macOS—it's the protocol family in a specific byte order, and I'm checking how WireGuard handles this to make sure I get it right. I'm working through the byte order confusion for the utun packet header—on little-endian systems, htonl(2) converts the value to network byte order, but I need to verify exactly how those bytes are laid out in memory to get the header format right. I'm looking at how WireGuard actually writes the protocol family byte to the TUN header—it seems they're placing the address family value directly at the offset position rather than using network byte order conversion, which would explain the discrepancy I was seeing with the endianness calculations. Looking at how VPN implementations handle this, I'm seeing that when writing to the utun interface, they prepend the address family as a 4-byte value in network byte order—so htonl(AF_INET) becomes 0x00 0x00 0x00 0x02—then use writev to send both the prefix and the packet data together. For reading, I need to understand the reverse process.
Verifying the byte order conversion... I'm working through the byte order conversions for the utun interface. When we write the address family using htonl(AF_INET), the kernel reads those bytes back and applies ntohl() to recover the original value, so the round-trip works correctly. On the read side, we're doing the same conversion in reverse to interpret the bytes the kernel sends us. So I need to actually look at the WireGuard-Go implementation on macOS to see how they handle this. Let me check their tun_darwin.go file to understand their approach with the AF field and how they're reading from the tun device. So the 4-byte prefix uses big-endian byte order—when I read those bytes as a big-endian uint32, I get the correct AF_INET value of 2. This means I need to use htonl() to convert the host byte order to network byte order when writing the address family field. So when utun writes those bytes, reading them back as a little-endian uint32 and converting with ntohl gives me AF_INET, which confirms my original implementation was correct. The byte ordering works out the same whether I use htonl(AF_INET) or construct the header directly.
Now I need to verify the exact function signature for chacha20_poly1305_decrypt from the library—it could return either bool or int depending on the implementation, and I need to handle the return value correctly when decrypting packets. I'm realizing there's a critical issue with my assumption about the return value—if this returns bool where true means success, then checking != 0 would actually indicate success, not failure. Looking at the OpenBSD implementation, it follows the standard POSIX convention of returning 0 on success, which is likely what macOS uses too. To be safe, I should check for < 0 instead of != 0 to handle both conventions properly. Looking at the exported functions in the object file, I can see both the standard and extended ChaCha20-Poly1305 variants are being used, particularly in the cookie handling code where these decryption functions are referenced as external symbols.
The OpenBSD implementation shows these functions return a boolean indicating successful authentication, so I'll declare them that way and check for failure accordingly. Since I need boolean support, I'll include the standard header for that.
Writing the implementation code...
For the network configuration on macOS, I need to configure the utun interface with an IP address and set up routes for the tunnel. The point-to-point setup requires assigning both local and peer addresses, then adding the appropriate route for the tunnel network. I should also check what the _noise_kdf function does since it's relevant here.
Looking at the Noise protocol functions, I see that _noise_kdf, _noise_mix_dh, and _noise_mix_psk are all internal to the library, so I'll need to implement my own versions of these. When handling the private key from the user's hex input, I need to make sure I'm clamping it the same way the library does during key generation—setting the appropriate bits for Curve25519 compatibility.
Writing the complete implementation now...
Actually, I should clarify the key handling flow: wg genkey already produces a properly clamped key, and since clamping is idempotent, applying it again won't hurt. The main thing to watch for is that WireGuard uses base64 encoding for keys while my demo uses hex, so there's a conversion step needed if someone wants to use keys generated by the Linux VM.
I could add base64 decoding support to make interop smoother, but for now I'll just document the conversion process in the README. Time to stop planning and actually write the code.
Writing the main C file... I'm organizing this into separate files for clarity—a main C implementation, a Makefile for compilation, and documentation. Now I'm wondering if libwg needs any system frameworks on macOS for the build to work properly.
Looking at the dependencies, the dispatch functions, POSIX threads, and random number generation are all part of the standard macOS system libraries that the linker should pick up automatically without needing explicit framework flags.
Now I'm ready to write out the complete WireGuard demo implementation...
Writing the C code... Including necessary headers for socket operations, system calls, and networking functionality...
Now I'm defining the BLAKE2s cryptographic hash state structure that needs to match the binary interface of the linked library, along with declaring the external functions from libwg.a for the streaming hash API.
Declaring the remaining cryptographic primitives... Defining WireGuard protocol constants and message types, along with the Noise protocol construction strings and MAC labels needed for the cryptographic handshake.
Now I'm laying out the packed struct definitions for the WireGuard message formats, starting with the initialization message that contains the sender index, ephemeral key, encrypted static key and timestamp, plus MAC fields for authentication.
Defining the response message structure... Defining the peer structure with session data and handshake state, then setting up BLAKE2s hashing helpers to mix data into the protocol's hash chain.
Still writing crypto functions... Implementing the KDF function that extracts a pseudorandom key via HMAC-BLAKE2s, then expands it into up to three output keys using counter-based iterations, clearing sensitive data when done.
Now I'm writing helper functions for generating TAI64N timestamps and computing keyed BLAKE2s MACs...
Implementing cryptographic utilities... Setting up the initiator message with the message type and sender index, then generating an ephemeral keypair and deriving the chain key from it before mixing the ephemeral public key into the handshake hash.
Performing Diffie-Hellman between the ephemeral and peer's static key to derive a new chain key and encryption key, then encrypting the initiator's static public key with that key and updating the hash. Now doing another DH between the initiator's static and peer's static keys to derive another chain key and key...
Deriving the next encryption key from that second DH, then encrypting a timestamp value and mixing it into the hash. Computing MAC1 over the message using the peer's public key, zeroing out MAC2, and clearing some temporary data.
Now I'm moving into the responder side of the handshake, where it processes an incoming initiation message. Setting up the initial chain key and hash state, then verifying the MAC1 value that came with the message.
Continuing with the key derivation steps...
Now I'm decrypting the peer's static key using ChaCha20-Poly1305, verifying it matches the expected peer public key, then mixing it into the hash state. After that, I'm performing another Diffie-Hellman operation between my static private key and the peer's static public key to derive the next round of cipher keys.
Decrypting the timestamp...
Now I'm moving into the responder's message creation. I need to set up the response message with the local and remote indices, then generate an ephemeral key pair for this side of the handshake. After that, I'm deriving the chaining key using KDF.
Continuing with the key derivation... Finishing the session key derivation for the responder, then computing the MAC1 authentication tag and clearing the temporary cryptographic values before returning.
Now moving to the initiator's side—processing the response message by deriving the chaining key from the responder's ephemeral public key and performing the corresponding Diffie-Hellman computation. Now I'm verifying the empty field with authenticated encryption, then deriving the session keys for both directions and extracting the remote index before clearing the temporary buffers.
Moving into the data path, I'm implementing packet encryption that first checks if the session is ready, then calculates the padding needed to align the plaintext to the proper block size. Finishing the padding logic, then encrypting the padded plaintext with ChaCha20-Poly1305 using the session key and nonce, incrementing the nonce afterward. Now I'm moving into the decryption function, which validates the session state and checks that the input is large enough to contain both the header and authentication tag.
Extracting the nonce from the packet header and setting up pointers to the encrypted data, then attempting to decrypt it and handling authentication failures by logging the nonce value that failed.
Now I'm determining the actual IP packet length by parsing the IP header version field—for IPv4 I extract the total length field, and for IPv6 I add 40 bytes to the payload length field—then validating that the result doesn't exceed the decrypted plaintext length before returning it.
Moving on to the macOS utun interface implementation, I'm setting up socket creation and control structures to open a utun interface by establishing a PF_SYSTEM socket and preparing to configure it with the Apple utun control name.
Still writing socket setup... Writing the write function... Writing helper functions for hex conversion... Finishing the base64 lookup table... Continuing with the base64 decoding logic... Finishing the base64 encoding logic, then moving into the main function where I'm setting up command-line argument handling with options for key generation, initialization, and responder modes. Continuing with the key generation logic...
Now I'm handling the command-line arguments to determine whether this is an initiator or responder mode, and setting up the peer structure accordingly.
Parsing command-line options... Parsing command-line arguments and validating required flags...
Now I'm deriving the public key from the private key and encoding it in both hex and base64 formats to display. Then I'm parsing the peer address to extract the IP and port for the initiator mode.
Opening the TUN interface and setting up the network configuration... Setting up the event loop with poll to monitor both the UDP socket and TUN device, then entering the main loop where I'll handle incoming packets and manage the WireGuard session state. Handling the UDP receive logic... Handling the decrypted packet data and then checking for TUN events to encrypt outgoing traffic...
Now I'm reviewing the complete implementation for potential issues, starting with the include dependencies.
I should replace the alloca call with a fixed stack-allocated buffer sized appropriately for the MTU plus padding alignment requirements.
Looking at the base64 table initialization, the designated initializer syntax should work fine in C99 and later—unspecified array elements default to zero, so the mapping from characters to their base64 values will be correct.
Now I'm verifying the decode logic for a 44-character base64 string with padding: 11 groups of 4 characters decode to 33 bytes nominally, but the final group with = padding only contributes 2 bytes, giving us exactly 32 bytes as expected.
Looking at the loop condition i < 43, I realize this processes groups starting at indices 0, 4, 8, ..., 40, which covers all 11 groups including the last one with padding. The issue is that = maps to 0 in the lookup table, which would corrupt the final byte calculation. I need to adjust the loop to handle the padded group correctly. Finishing the last group extraction with the padding character, I get 18 bits of data which yields 2 complete bytes plus some extra bits I need to discard. The loop extracts these bytes by shifting and masking the value, and after processing 10 full groups I'm at byte 30, so the final group at index 40 will write bytes 30 and 31 to complete the 32-byte output. j starts at 30:

out[30] = (v >> 16): j becomes 31
out[31] = (v >> 8): j becomes 32
out[32]: j < 32 is false, skipped ✓

So the decode works correctly. The loop condition i < 43 is wrong though - we want i <= 40 (process groups at 0,4,8,...,40). i < 43 would give us groups at i=0,4,...,40, then the loop checks i=44 >= 43, stops. Actually wait, the loop increments i by 4:

i=0, 4, 8, 12, 16, 20, 24, 28, 32, 36, 40 (11 iterations)
i=44, check i < 43 → false, loop ends ✓

Good, the loop is correct.
The lookup table uses int8_t values cast to uint8_t when indexing, which works fine since all the base64 values are in the 0-63 range. The designated initializers like ['A'] = 0 are explicit but harmless since uninitialized elements default to zero anyway. Now I need to handle the endianness functions - htole32 and le32toh might not be available on macOS without the right headers, so I should either use __builtin_bswap32 or define them explicitly for little-endian systems. Finishing the inline functions for endianness conversion...
For the string constants, I'm using sizeof minus one to get the length at compile time rather than calling strlen, which is cleaner since these are static strings with known sizes.
Now I'm verifying the encryption and decryption flow for the static key material—the wg_create_init function encrypts 32 bytes of the peer's public key with a 16-byte authentication tag, producing the 48-byte static_enc field, and wg_consume_init decrypts it back using the same parameters.
The timestamp encryption follows the same pattern with 12 bytes of plaintext plus a 16-byte tag equaling 28 bytes total. For the empty ciphertext case in wg_consume_resp, I'm decrypting a 16-byte tag-only message into a minimal buffer since there's no actual plaintext to recover. I'm checking whether passing NULL data to the HMAC function during session key derivation will work correctly—BLAKE2s should handle zero-byte updates gracefully, but I should verify this doesn't cause issues.
For the AEAD encryption of empty messages, the implementation should produce just the authentication tag without problems. I've also replaced alloca with a fixed-size buffer to avoid platform-specific header issues, so the code should be portable. Now I need to finalize the Makefile and...
Actually, let me double-check the noise_kdf calls for session key derivation and verify the bootstrap logic is correct for both initiator and responder. The key concern is whether hs_ck and hs_h are being saved from the right state variables, and whether the responder's bootstrap correctly uses our public key while the initiator uses the peer's public key.
Looking at the bootstrap calls, both sides are correctly using the responder's public key as the starting point—the initiator passes the peer's public key (since the peer is the responder), and the responder passes its own public key. This ensures they're in sync.
Now I'm checking the noise_kdf invocation where we're using the same pointer for both input and output...
Examining the KDF logic... I'm verifying the aliasing is safe—key points to p->hs_ck during the HMAC computation, but it's only read, not written, so overwriting p->hs_ck afterward through t1 doesn't cause any issues. The code checks out, so I'm ready to write the final clean version.
Now I'm putting together the complete deliverables: the C implementation, a Makefile, and setup instructions for Linux. I should also note that on macOS, creating the utun device requires root privileges or proper entitlements, so users would need to run with sudo.
Refining the buffer allocation approach...
I'm realizing that a 64KB stack buffer is too risky across different platforms, so I should use dynamic allocation with malloc instead to handle the padded packet safely. Writing the MAC1 verification logic...
