# Work Log - 2026-04-11

## 目标
- 补齐 `Makefile` / `xcframework` 的 iOS 与多平台构建能力。
- 修复 `XCFramework` 在 Xcode 的加载问题。
- 增加双路由模式（Full Tunnel / Split Tunnel），并优化 UI 交互。
- 修复 `PacketTunnelProvider` 的 UDP 建链与路由策略问题。
- 增强 `handleAppMessage(get=1)` 诊断信息。

## 本次完成

### 1. 构建系统
- `Makefile`
  - `all` 调整为仅构建基础库（`libwg.a` + `libswift_crypto.a`）。
  - 增加 `build-ios` 目标：构建后验证 iOS device/simulator slice。
  - 更新 `help` 说明。
- `scripts/build-xcframework.sh`
  - 支持：macOS / iOS / tvOS / visionOS（工具链支持时）。
  - 统一产物组织，增加 slice 组合与检查。
  - 修复 `Info.plist`：framework 类型库去掉不兼容的 `HeadersPath` 字段。

### 2. Sample App 与工程配置
- `WireGuardSampleApp/project.yml`
  - 依赖改为 `WireGuardCore.xcframework` 根目录引用，避免只引用单一 macOS slice。
- 重新生成 `project.pbxproj` 和共享 scheme。

### 3. UI 与跨平台兼容
- `ContentView.swift`
  - macOS 使用 `HSplitView`，iOS 等平台使用兼容布局。
  - 增加路由模式切换：`Full Tunnel` / `Split Tunnel`。
  - 优化视觉层次与背景样式。

### 4. 路由与数据面修复（核心）
- `TunnelManager.swift`
  - 新增 `RouteMode`，保存到 `providerConfiguration["routeMode"]`。
- `PacketTunnelProvider.swift`
  - 修复 UDP 会话未建立问题：
    - 优先从 session introspection 读取 peer endpoint。
    - 若读取不到，回退解析配置里的 `Endpoint`。
  - 修复全局路由回环：
    - Full 模式：默认接管并排除 outer endpoint。
    - Split 模式：不改系统 default route，只注入目标网段。
  - 增加 endpoint/network 工具函数：
    - `firstPeerEndpoint(for:)`
    - `firstPeerEndpointFromConfig(_:)`
    - `networkAddress(of:prefixLength:)`
  - 开发环境增强：同网段自动源地址绑定（`createUDPSession(to:from:)`）。
    - VMware bridge 场景下将源 IP 从 `192.168.112.201` 修正为 `172.16.203.1`。
  - `handleAppMessage(get=1)` 输出新增：
    - `route_mode`
    - `tunnel_remote`
    - `peer_host`
    - `selected_endpoint`
    - `selected_local_bind`
    - `udp_session`
    - included/excluded 路由明细与计数。

## 关键验证结果
- `make xcframework` 成功，iOS/tvOS/macOS slice 正常，visionOS 依赖本机工具链能力自动判定。
- Xcode 不再报：`HeadersPath is not supported for a framework`。
- 抓包从异常：
  - `192.168.112.201:* -> 172.16.203.128:51820`
- 修复后：
  - `172.16.203.1:* -> 172.16.203.128:51820`
  - 服务器开始回包。
- `ping 10.88.0.1` 从连续超时恢复为可达（间歇丢包后稳定）。

## 结论
- 已实现双模式路由与多平台构建目标。
- 已修复导致 `tx/rx=0`、握手重传、路由回环、开发环境源地址错误的关键问题。
- 当前版本适合继续进入回归测试与真实网络环境验证。
