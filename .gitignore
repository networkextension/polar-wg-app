# Build artifacts
build/
*.o
*.a

# Legacy / stray test binaries living next to sources
src/wg_client_final
src/wg_macos_tool
src/crypto_vector_test
src/libswift_crypto.a

# Secrets — never commit
*.key
src/client_private.key
src/client_public.key
src/client.conf
src/client_tool.conf
src/client_wgset.conf
src/id_ed25519
src/id_ed25519.pub
src/test.ini
src/server_tcpdump.txt

# join-windows.ps1 output: real private keys + registration tokens
config/

# macOS cruft
.DS_Store

# Xcode user-specific state (the project.pbxproj is checked in but
# user-local Xcode breadcrumbs are not)
**/xcuserdata/
**/*.xcuserstate
**/project.xcworkspace/xcuserdata/

# Swift package + cross-arch bundle build artifacts
.build/
build-x86/
build-uni/

# Release bundles / APKs (make-bundle.sh, gradle release output)
dist/
WireGuardAndroid/app/release/

# Local scratch scripts (session resumes, ad-hoc tunnel test loops)
newhub.sh
resume.sh
test.script.sh
update_test.sh
src/script.sh
src/stop.sh
src/wg_client.c
src/wg_client2.c
doc/test.md

# Ad-hoc peer test scripts that embed literal private keys
src/test-go.sh
src/test-go2.sh
